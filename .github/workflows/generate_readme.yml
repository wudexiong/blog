name: git issues blog workflow

on: 
  push:
    branches:
      - main
    paths:
      - 'posts/*.md'
      - 'posts/*/*.md'
      - 'posts/*/*/*.md'


jobs:
  git-issues-blog:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
      with:
        ref: main
    - name: Publish Issues
      uses: ./
      env:
        GITHUB_TOKEN: ${{ secrets.G_T }}
        POSTS_PATH: 'posts'
